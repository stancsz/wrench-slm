# Local context integration screen 01 evaluation

**Disposition: inadmissible diagnostic. No local task class is accepted.**

The retained five-case receipt records all expected mechanics branches (1
required-source prompt-ready case and 4 fail-closed cases). Independent review
confirmed the receipt, exposure marker, fixture, runner, protocol, pipeline
hashes, and case results agree. However, the protocol required a retained
orchestrator admission record before execution. That record is absent, so its
explicit `NO RUN` rule applies. These observations cannot establish a valid
screen result or acceptance.

The fixture is exposed and must never be rerun. The receipt also omits the
positive case's snapshot hash, preventing independent recomputation of its
evidence ID from the retained artifact. The pinned runner performed that check
during execution. The positive character count (720) is a fixture proxy, not
model tokens. No model, tokenizer, client, provider, network, or downstream
task ran. Frontier-token savings: **N/A**, zero matched usage pairs and zero
independently verified paired successes.

The report is [here](../../reports/wrench-local-acceptability/local-context-integration-screen-01.md).
The governing protocol is [here](local-context-integration-screen-01-protocol.md).
