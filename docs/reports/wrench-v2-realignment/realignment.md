# V2 realignment work report

Status: complete for the bounded realignment
Author: root agent, 2026-09-23 (America/Edmonton)
Goal: [realignment](../../goal/wrench-v2-realignment/GOAL.md)

## Work produced

Replaced competing active root guidance with the Layer 1 and continuous-LoRA
direction; defined the architecture, experiment, v1 lessons and storage/recovery
policy. Preserved both supplied notes and eight exact earlier direction files.
Pinned complete upstream model metadata without downloading weights.

Moved eight loose docs into `docs/misc/v1/`, rebased live navigation and updated
source fixture paths. Marked the old corpus direction as superseded. Preserved
historical evaluation rows, receipt contents and unrelated source edits. Added
scratch/environment ignore rules and a bilingual site experiment page with
clear notices on v1 material. The parent contract carries no v2 paid allowance.

The [archive manifests](../../archive/2026-09-23-v1/README.md) identify snapshots
and relocated documents. The independent [reuse audit](reuse-audit.md) records
the retained primitives and missing v2 capabilities.

## Verification and remaining work

The [review record](../../evals/wrench-v2-realignment/review.md) records 54
passing focused tests, site/link/hash checks, two repaired test fixtures, the
independent PASS and the unrun PyTorch-dependent full-suite limitation. Initial storage
inventory was 582,485,162 bytes, with no prior active reservations. Root
reserved 20,000,000 peak additional bytes for this bounded cleanup. Separate
reservations bounded dependencies (40 MB), checks (10 MB) and site output
(5 MB). The resulting inventoried footprint is approximately 0.59 GB; retained
test dependencies account for about 6.18 MB. Job reservations are released
after work stops and files are accounted for.

No model download, training, inference, paid request, filesystem repair,
production activation, commit or publication occurred. V2 E0 through E4 remain
planned. In particular, adapter composition, continual learning, physical-disk
headroom enforcement in job runners and recovery drills remain implementation
work, not guarantees supplied by these documents.
