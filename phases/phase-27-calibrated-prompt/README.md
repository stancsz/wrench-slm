# Phase 27: calibrated proposal prompt probe

This phase probes whether the packed tiers can follow the explicit Wrench
proposal contract when given the same system-level schema and safety rules used
by the real adapter path. It is a development probe, not a final evaluation or
training corpus. The portfolio remains `pending_human_approval`.
