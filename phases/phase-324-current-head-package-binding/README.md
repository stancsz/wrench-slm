# Phase 324: direct-probe package binding

The latest source commit `9cc0c68` was rematerialized as:
`D:\models\_wrench-release-candidate-9cc0c68`.

The candidate passed structural validation and bundles the updated native
verifier, including `--probe-payload-tokens` for direct 2M or 4M raw-input
accounting and the resource-safe Windows process-tree cleanup.

This is the package bound for the next direct-input or independent-worker run.
It remains an Experimental Preview candidate, not a publication or production
approval.

Evidence: `phases/phase-324-current-head-package-validation.json`.
