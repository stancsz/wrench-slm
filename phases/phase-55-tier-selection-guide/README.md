# Phase 55: tier selection guide

The repository now has a current, user-facing selection guide for the two
verified quantized artifacts. It records their actual packed sizes, guarded
fixture results, local paths, fallback safeguards, and the remaining release
gates. The guide is `docs/WRENCH_MODEL_TIERS.md`.
